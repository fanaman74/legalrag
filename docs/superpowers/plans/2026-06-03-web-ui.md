# Legal RAG Web UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a password-protected web management UI (upload, list, delete, search) served by the existing Railway Express server, plus EML/MSG email parsing support.

**Architecture:** New `src/api.ts` Express Router (REST + file upload) mounted at `/api` in `remote.ts`; static `public/index.html` (vanilla JS + Tailwind CDN) served from the same Express process; `src/ingest.ts` extended with `parseEml`/`parseMsg`; `src/tools.ts` updated for `.eml`/`.msg` branches.

**Tech Stack:** Express, multer (memory storage), jsonwebtoken, mailparser, @kenjiuno/msgreader, Tailwind CDN, vanilla JS

---

## File Map

```
src/
  api.ts          ← NEW: REST router (/api/auth, /api/documents, /api/ingest, /api/search)
  ingest.ts       ← MODIFY: add parseEml(), parseMsg()
  tools.ts        ← MODIFY: add .eml/.msg branches in handleIngestFile()
  remote.ts       ← MODIFY: mount /api router, serve public/
public/
  index.html      ← NEW: full frontend (login + 3-tab app)
tests/
  ingest-parsers.test.ts  ← NEW: unit tests for parseEml / parseMsg
.env              ← MODIFY: add ADMIN_PASSWORD
.env.example      ← MODIFY: add ADMIN_PASSWORD=
```

---

## Task 1: Git init + install dependencies + update env

**Files:**
- Modify: `package.json`
- Modify: `.env`
- Modify: `.env.example`

- [ ] **Step 1: Initialise git repo**

```bash
cd /Users/fred/Documents/VibeCoding/claudecode/mylegal-mcp
git init
git add -A
git commit -m "chore: initial commit — legal-doc-rag MCP server"
```

Expected: `master` branch created, all existing files committed.

- [ ] **Step 2: Install new production dependencies**

```bash
npm install mailparser @kenjiuno/msgreader multer jsonwebtoken
```

- [ ] **Step 3: Install new dev dependencies**

```bash
npm install --save-dev @types/mailparser @types/multer @types/jsonwebtoken
```

- [ ] **Step 4: Add ADMIN_PASSWORD to .env**

Open `/Users/fred/Documents/VibeCoding/claudecode/mylegal-mcp/.env` and add this line at the end:

```
ADMIN_PASSWORD=your-strong-passphrase-here
```

Replace `your-strong-passphrase-here` with a real passphrase (min 12 chars recommended).

- [ ] **Step 5: Add ADMIN_PASSWORD to .env.example**

Open `/Users/fred/Documents/VibeCoding/claudecode/mylegal-mcp/.env.example` and add:

```
ADMIN_PASSWORD=
```

- [ ] **Step 6: Verify TypeScript still compiles**

```bash
cd /Users/fred/Documents/VibeCoding/claudecode/mylegal-mcp && npx tsc --noEmit 2>&1
```

Expected: no errors.

- [ ] **Step 7: Commit**

```bash
git add package.json package-lock.json .env.example
git commit -m "chore: add web UI dependencies and ADMIN_PASSWORD env var"
```

---

## Task 2: parseEml and parseMsg (TDD)

**Files:**
- Create: `tests/ingest-parsers.test.ts`
- Modify: `src/ingest.ts`

- [ ] **Step 1: Write failing tests**

Create `/Users/fred/Documents/VibeCoding/claudecode/mylegal-mcp/tests/ingest-parsers.test.ts`:

```typescript
// tests/ingest-parsers.test.ts
import { describe, it, expect } from 'vitest';
import { parseEml, parseMsg } from '../src/ingest.js';

describe('parseEml', () => {
  it('extracts from, to, subject and body from a plain-text EML', async () => {
    const eml = [
      'From: Alice <alice@example.com>',
      'To: Bob <bob@example.com>',
      'Date: Thu, 15 Nov 2025 10:00:00 +0000',
      'Subject: Contract Review',
      'Content-Type: text/plain; charset=utf-8',
      '',
      'Please review the attached contract by Friday.',
    ].join('\r\n');

    const text = await parseEml(Buffer.from(eml));
    expect(text).toContain('alice@example.com');
    expect(text).toContain('bob@example.com');
    expect(text).toContain('Contract Review');
    expect(text).toContain('Please review the attached contract');
  });

  it('falls back to stripping HTML when no plain-text part', async () => {
    const eml = [
      'From: sender@example.com',
      'To: recipient@example.com',
      'Subject: HTML Only',
      'Content-Type: text/html; charset=utf-8',
      '',
      '<html><body><p>HTML body content here</p></body></html>',
    ].join('\r\n');

    const text = await parseEml(Buffer.from(eml));
    expect(text).toContain('HTML body content here');
  });

  it('output starts with From: line', async () => {
    const eml = [
      'From: sender@example.com',
      'To: recipient@example.com',
      'Subject: Test',
      'Content-Type: text/plain',
      '',
      'Body text.',
    ].join('\r\n');

    const text = await parseEml(Buffer.from(eml));
    expect(text.startsWith('From:')).toBe(true);
  });
});

describe('parseMsg', () => {
  it('throws a meaningful error for non-MSG data', async () => {
    const badBuffer = Buffer.from('this is not a msg file');
    await expect(parseMsg(badBuffer)).rejects.toThrow();
  });
});
```

- [ ] **Step 2: Run tests — verify they fail**

```bash
cd /Users/fred/Documents/VibeCoding/claudecode/mylegal-mcp && npm test 2>&1
```

Expected: 3 of 4 new tests FAIL with "parseEml is not exported from '../src/ingest.js'". The existing 5 chunking tests should still pass.

- [ ] **Step 3: Add parseEml and parseMsg to src/ingest.ts**

Add these two functions at the end of `/Users/fred/Documents/VibeCoding/claudecode/mylegal-mcp/src/ingest.ts`:

```typescript
export async function parseEml(buffer: Buffer): Promise<string> {
  const { simpleParser } = await import('mailparser');
  const parsed = await simpleParser(buffer);

  const from    = parsed.from?.text    ?? '';
  const to      = parsed.to?.text      ?? '';
  const date    = parsed.date ? parsed.date.toISOString().slice(0, 10) : '';
  const subject = parsed.subject       ?? '';

  // Prefer plain text; strip HTML tags if only HTML is available
  let body = parsed.text ?? '';
  if (!body && parsed.html) {
    body = parsed.html.replace(/<[^>]*>/g, ' ').replace(/\s{2,}/g, ' ').trim();
  }

  return [
    from    ? `From: ${from}`    : '',
    to      ? `To: ${to}`        : '',
    date    ? `Date: ${date}`    : '',
    subject ? `Subject: ${subject}` : '',
    '',
    body,
  ].filter((line, idx) => idx >= 4 || line !== '').join('\n');
}

export async function parseMsg(buffer: Buffer): Promise<string> {
  const { default: MsgReader } = await import('@kenjiuno/msgreader');
  const reader = new MsgReader(buffer);
  const data   = reader.getFileData();

  const from    = (data as any).senderEmail ?? (data as any).senderName ?? '';
  const to      = ((data as any).recipients ?? [])
    .map((r: any) => r.email ?? r.name ?? '')
    .filter(Boolean)
    .join(', ');
  const subject = (data as any).subject ?? '';
  const body    = (data as any).body    ?? '';

  return [
    from    ? `From: ${from}`       : '',
    to      ? `To: ${to}`           : '',
    subject ? `Subject: ${subject}` : '',
    '',
    body,
  ].filter((line, idx) => idx >= 3 || line !== '').join('\n');
}
```

- [ ] **Step 4: Run tests — verify they pass**

```bash
cd /Users/fred/Documents/VibeCoding/claudecode/mylegal-mcp && npm test 2>&1
```

Expected: 9 tests pass (5 chunking + 4 parser tests). The `parseMsg` error test should pass because invalid data causes MsgReader to throw.

- [ ] **Step 5: TypeScript compile check**

```bash
cd /Users/fred/Documents/VibeCoding/claudecode/mylegal-mcp && npx tsc --noEmit 2>&1
```

Expected: no errors.

- [ ] **Step 6: Commit**

```bash
git add src/ingest.ts tests/ingest-parsers.test.ts
git commit -m "feat: add parseEml and parseMsg to ingest pipeline"
```

---

## Task 3: Update src/tools.ts — .eml and .msg support

**Files:**
- Modify: `src/tools.ts` (lines ~99–102)

- [ ] **Step 1: Update handleIngestFile to handle .eml and .msg**

In `/Users/fred/Documents/VibeCoding/claudecode/mylegal-mcp/src/tools.ts`, find the `handleIngestFile` function. It currently reads:

```typescript
  let rawText: string;
  if (ext === '.pdf')       rawText = await parsePdf(buffer);
  else if (ext === '.docx') rawText = await parseDocx(buffer);
  else                      rawText = buffer.toString('utf-8');
```

Replace with:

```typescript
  let rawText: string;
  if (ext === '.pdf')       rawText = await parsePdf(buffer);
  else if (ext === '.docx') rawText = await parseDocx(buffer);
  else if (ext === '.eml')  rawText = await parseEml(buffer);
  else if (ext === '.msg')  rawText = await parseMsg(buffer);
  else                      rawText = buffer.toString('utf-8');
```

Also update the import at the top of `tools.ts`. Find:

```typescript
import { ingestDocument, parsePdf, parseDocx } from './ingest.js';
```

Replace with:

```typescript
import { ingestDocument, parsePdf, parseDocx, parseEml, parseMsg } from './ingest.js';
```

- [ ] **Step 2: TypeScript compile check**

```bash
cd /Users/fred/Documents/VibeCoding/claudecode/mylegal-mcp && npx tsc --noEmit 2>&1
```

Expected: no errors.

- [ ] **Step 3: Run tests**

```bash
cd /Users/fred/Documents/VibeCoding/claudecode/mylegal-mcp && npm test 2>&1
```

Expected: 9/9 pass.

- [ ] **Step 4: Commit**

```bash
git add src/tools.ts
git commit -m "feat: add .eml and .msg support to ingest_file tool"
```

---

## Task 4: Create src/api.ts — REST router

**Files:**
- Create: `src/api.ts`

- [ ] **Step 1: Create /Users/fred/Documents/VibeCoding/claudecode/mylegal-mcp/src/api.ts**

```typescript
// src/api.ts
import express from 'express';
import multer from 'multer';
import jwt from 'jsonwebtoken';
import path from 'path';
import { listDocuments, deleteDocument, semanticSearch } from './supabase.js';
import { ingestDocument, parsePdf, parseDocx, parseEml, parseMsg } from './ingest.js';
import { embedText } from './embeddings.js';
import type { SourceType } from './types.js';

const router = express.Router();

const upload = multer({
  storage: multer.memoryStorage(),
  limits: { fileSize: 20 * 1024 * 1024 }, // 20 MB
});

const ALLOWED_EXTS = new Set(['.pdf', '.docx', '.txt', '.eml', '.msg']);

function getSecret(): string {
  const s = process.env.ADMIN_PASSWORD;
  if (!s) throw new Error('ADMIN_PASSWORD env var is not set');
  return s;
}

// ─── Auth middleware ────────────────────────────────────────────────────────
function requireAuth(
  req: express.Request,
  res: express.Response,
  next: express.NextFunction
): void {
  const auth = req.headers.authorization;
  if (!auth?.startsWith('Bearer ')) {
    res.status(401).json({ error: 'Unauthorized' });
    return;
  }
  try {
    jwt.verify(auth.slice(7), getSecret());
    next();
  } catch {
    res.status(401).json({ error: 'Invalid or expired token' });
  }
}

// ─── POST /api/auth ─────────────────────────────────────────────────────────
router.post('/auth', (req, res) => {
  const { password } = req.body as { password?: string };
  if (!password || password !== process.env.ADMIN_PASSWORD) {
    res.status(401).json({ error: 'Invalid password' });
    return;
  }
  const token = jwt.sign({ sub: 'admin' }, getSecret(), { expiresIn: '24h' });
  res.json({ token });
});

// ─── GET /api/documents ─────────────────────────────────────────────────────
router.get('/documents', requireAuth, async (_req, res) => {
  try {
    const docs = await listDocuments();
    res.json(docs);
  } catch (err) {
    res.status(500).json({ error: (err as Error).message });
  }
});

// ─── DELETE /api/documents/:id ──────────────────────────────────────────────
router.delete('/documents/:id', requireAuth, async (req, res) => {
  const id = parseInt(req.params.id, 10);
  if (isNaN(id)) {
    res.status(400).json({ error: 'Invalid document ID' });
    return;
  }
  try {
    await deleteDocument(id);
    res.json({ message: `Document ${id} deleted.` });
  } catch (err) {
    res.status(500).json({ error: (err as Error).message });
  }
});

// ─── POST /api/ingest ───────────────────────────────────────────────────────
router.post('/ingest', requireAuth, upload.single('file'), async (req, res) => {
  if (!req.file) {
    res.status(400).json({ error: 'No file uploaded' });
    return;
  }

  const { source_type, case_number, document_date, language } = req.body as {
    source_type?: string;
    case_number?: string;
    document_date?: string;
    language?: string;
  };

  if (!source_type) {
    res.status(400).json({ error: 'source_type is required' });
    return;
  }

  const ext = path.extname(req.file.originalname).toLowerCase();
  if (!ALLOWED_EXTS.has(ext)) {
    res.status(400).json({ error: `Unsupported file type: ${ext}` });
    return;
  }

  try {
    const buffer = req.file.buffer;

    let rawText: string;
    if (ext === '.pdf')       rawText = await parsePdf(buffer);
    else if (ext === '.docx') rawText = await parseDocx(buffer);
    else if (ext === '.eml')  rawText = await parseEml(buffer);
    else if (ext === '.msg')  rawText = await parseMsg(buffer);
    else                      rawText = buffer.toString('utf-8');

    const result = await ingestDocument({
      filename:     req.file.originalname,
      rawText,
      sourceType:   source_type as SourceType,
      caseNumber:   case_number  || undefined,
      documentDate: document_date || undefined,
      language:     (language as 'en' | 'fr') ?? 'en',
    });

    res.json({
      message:       result.message,
      documentId:    result.documentId,
      chunksCreated: result.chunksCreated,
    });
  } catch (err) {
    res.status(500).json({ error: (err as Error).message });
  }
});

// ─── GET /api/search ────────────────────────────────────────────────────────
router.get('/search', requireAuth, async (req, res) => {
  const { q, source_type, case_number, threshold, limit } = req.query as Record<string, string>;

  if (!q) {
    res.status(400).json({ error: 'q (query) is required' });
    return;
  }

  try {
    const queryEmbedding = await embedText(q);
    const results = await semanticSearch(queryEmbedding, {
      sourceType: source_type  || undefined,
      caseNumber: case_number  || undefined,
      threshold:  threshold    ? parseFloat(threshold) : 0.60,
      limit:      limit        ? parseInt(limit, 10)   : 8,
    });
    res.json(results);
  } catch (err) {
    res.status(500).json({ error: (err as Error).message });
  }
});

export default router;
```

- [ ] **Step 2: TypeScript compile check**

```bash
cd /Users/fred/Documents/VibeCoding/claudecode/mylegal-mcp && npx tsc --noEmit 2>&1
```

Expected: no errors.

- [ ] **Step 3: Run tests**

```bash
cd /Users/fred/Documents/VibeCoding/claudecode/mylegal-mcp && npm test 2>&1
```

Expected: 9/9 pass.

- [ ] **Step 4: Commit**

```bash
git add src/api.ts
git commit -m "feat: add REST API router (auth, documents, ingest, search)"
```

---

## Task 5: Update src/remote.ts — mount API + serve static

**Files:**
- Modify: `src/remote.ts`

- [ ] **Step 1: Update src/remote.ts**

Replace the entire content of `/Users/fred/Documents/VibeCoding/claudecode/mylegal-mcp/src/remote.ts` with:

```typescript
// src/remote.ts
import 'dotenv/config';
import express from 'express';
import cors from 'cors';
import { fileURLToPath } from 'url';
import { join, dirname } from 'path';
import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import { SSEServerTransport } from '@modelcontextprotocol/sdk/server/sse.js';
import {
  searchDocumentsSchema, handleSearchDocuments,
  ingestTextSchema,      handleIngestText,
  ingestFileSchema,      handleIngestFile,
  deleteDocumentSchema,  handleDeleteDocument,
  handleListDocuments,
} from './tools.js';
import apiRouter from './api.js';

const __dirname = dirname(fileURLToPath(import.meta.url));

const app = express();
app.use(cors());
app.use(express.json({ limit: '10mb' }));

// ─── REST API ────────────────────────────────────────────────────────────────
app.use('/api', apiRouter);

// ─── Static frontend ─────────────────────────────────────────────────────────
app.use(express.static(join(__dirname, '..', 'public')));
app.get('*', (req, res) => {
  if (
    !req.path.startsWith('/api') &&
    !req.path.startsWith('/sse') &&
    !req.path.startsWith('/messages')
  ) {
    res.sendFile(join(__dirname, '..', 'public', 'index.html'));
  }
});

// ─── MCP / SSE ───────────────────────────────────────────────────────────────
function buildMcpServer(): McpServer {
  const server = new McpServer({ name: 'legal-doc-rag', version: '1.0.0' });

  server.tool('search_documents',  'Semantic search across legal documents.',          searchDocumentsSchema.shape, async (input) => ({ content: [{ type: 'text' as const, text: await handleSearchDocuments(input) }] }));
  server.tool('ingest_text',       'Add plain-text document to the RAG database.',     ingestTextSchema.shape,      async (input) => ({ content: [{ type: 'text' as const, text: await handleIngestText(input) }] }));
  server.tool('ingest_file',       'Add PDF/DOCX/TXT from disk to the RAG database.',  ingestFileSchema.shape,      async (input) => ({ content: [{ type: 'text' as const, text: await handleIngestFile(input) }] }));
  server.tool('list_documents',    'List all indexed documents.',                      {},                          async () => ({ content: [{ type: 'text' as const, text: await handleListDocuments() }] }));
  server.tool('delete_document',   'Delete a document from the RAG database.',         deleteDocumentSchema.shape,  async (input) => ({ content: [{ type: 'text' as const, text: await handleDeleteDocument(input) }] }));

  return server;
}

const sessions = new Map<string, SSEServerTransport>();

app.get('/sse', async (req, res) => {
  const transport = new SSEServerTransport('/messages', res);
  sessions.set(transport.sessionId, transport);
  res.on('close', () => sessions.delete(transport.sessionId));

  const server = buildMcpServer();
  await server.connect(transport);
});

app.post('/messages', async (req, res) => {
  const sessionId = req.query['sessionId'] as string;
  const transport = sessions.get(sessionId);
  if (!transport) {
    res.status(404).json({ error: 'Session not found' });
    return;
  }
  await transport.handlePostMessage(req, res);
});

app.get('/health', (_, res) => res.json({ status: 'ok' }));

const port = process.env.PORT ?? 3000;
app.listen(port, () => console.log(`[legal-doc-rag] HTTP MCP server on :${port}`));
```

- [ ] **Step 2: TypeScript compile check**

```bash
cd /Users/fred/Documents/VibeCoding/claudecode/mylegal-mcp && npx tsc --noEmit 2>&1
```

Expected: no errors.

- [ ] **Step 3: Run tests**

```bash
cd /Users/fred/Documents/VibeCoding/claudecode/mylegal-mcp && npm test 2>&1
```

Expected: 9/9 pass.

- [ ] **Step 4: Commit**

```bash
git add src/remote.ts
git commit -m "feat: mount REST API router and serve web UI from remote server"
```

---

## Task 6: Create public/index.html — frontend

**Files:**
- Create: `public/index.html`

- [ ] **Step 1: Create public directory**

```bash
mkdir -p /Users/fred/Documents/VibeCoding/claudecode/mylegal-mcp/public
```

- [ ] **Step 2: Create /Users/fred/Documents/VibeCoding/claudecode/mylegal-mcp/public/index.html**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Legal RAG — Document Manager</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 min-h-screen">

<!-- ═══════════════════ LOGIN ═══════════════════ -->
<div id="loginView" class="min-h-screen flex items-center justify-center">
  <div class="bg-white shadow-md rounded-2xl p-8 w-full max-w-sm">
    <h1 class="text-2xl font-bold text-gray-800 mb-1 text-center">Legal RAG</h1>
    <p class="text-sm text-gray-400 text-center mb-6">Document Manager</p>
    <form id="loginForm">
      <label class="block text-sm font-medium text-gray-700 mb-1">Password</label>
      <input id="loginPassword" type="password" required autocomplete="current-password"
        class="w-full border border-gray-300 rounded-lg px-3 py-2 mb-3 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500" />
      <div id="loginError" class="text-red-600 text-sm mb-3 hidden"></div>
      <button type="submit"
        class="w-full bg-blue-600 hover:bg-blue-700 text-white font-medium py-2 rounded-lg text-sm transition">
        Login
      </button>
    </form>
  </div>
</div>

<!-- ═══════════════════ MAIN APP ═══════════════════ -->
<div id="mainApp" class="hidden min-h-screen flex flex-col">

  <!-- Header -->
  <header class="bg-white border-b border-gray-200 px-6 py-4 flex items-center justify-between shrink-0">
    <h1 class="text-lg font-bold text-gray-800">Legal RAG — Document Manager</h1>
    <button id="logoutBtn" class="text-sm text-gray-400 hover:text-red-600 transition">Logout</button>
  </header>

  <!-- Tab bar -->
  <div class="bg-white border-b border-gray-200 px-6 shrink-0">
    <nav class="flex gap-6">
      <button data-tab="upload"    class="tab-btn py-3 text-sm font-medium">Upload</button>
      <button data-tab="documents" class="tab-btn py-3 text-sm font-medium">Documents</button>
      <button data-tab="search"    class="tab-btn py-3 text-sm font-medium">Search</button>
    </nav>
  </div>

  <!-- Content -->
  <main class="flex-1 overflow-auto">
    <div class="max-w-5xl mx-auto px-6 py-8">

      <!-- ─── UPLOAD TAB ─── -->
      <div id="tab-upload">

        <!-- Drop zone + mode buttons -->
        <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6 mb-4">
          <div class="flex gap-2 mb-4">
            <button id="modeFiles"
              class="px-4 py-1.5 rounded-full text-sm font-medium bg-blue-600 text-white transition">
              Files
            </button>
            <button id="modeFolder"
              class="px-4 py-1.5 rounded-full text-sm font-medium bg-gray-100 text-gray-600 hover:bg-gray-200 transition">
              Folder
            </button>
          </div>

          <input id="fileInput"   type="file" multiple accept=".pdf,.docx,.txt,.eml,.msg" class="hidden" />
          <input id="folderInput" type="file" webkitdirectory class="hidden" />

          <div id="dropZone"
            class="border-2 border-dashed border-gray-300 rounded-xl p-10 text-center cursor-pointer hover:border-blue-400 hover:bg-blue-50 transition">
            <p class="text-gray-500 text-sm">Drop files here or <span class="text-blue-600 font-medium">click to browse</span></p>
            <p class="text-xs text-gray-400 mt-1">PDF · DOCX · TXT · EML · MSG</p>
          </div>

          <p id="selectedCount" class="mt-2 text-sm text-gray-500 hidden"></p>
        </div>

        <!-- Metadata form -->
        <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6 mb-4">
          <h3 class="text-sm font-semibold text-gray-700 mb-4">Metadata — applies to all files in this batch</h3>
          <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
            <div>
              <label class="block text-xs font-medium text-gray-600 mb-1">Source Type *</label>
              <select id="sourceType" class="w-full border border-gray-300 rounded-lg px-3 py-2 text-sm">
                <option value="olaf_complaint">OLAF Complaint</option>
                <option value="general_court">General Court</option>
                <option value="ias_audit">IAS Audit</option>
                <option value="ep_discharge">EP Discharge</option>
                <option value="correspondence" selected>Correspondence</option>
                <option value="staff_regulations">Staff Regulations</option>
                <option value="decision">Decision</option>
                <option value="other">Other</option>
              </select>
            </div>
            <div>
              <label class="block text-xs font-medium text-gray-600 mb-1">Language</label>
              <select id="language" class="w-full border border-gray-300 rounded-lg px-3 py-2 text-sm">
                <option value="en">English</option>
                <option value="fr">French</option>
              </select>
            </div>
            <div>
              <label class="block text-xs font-medium text-gray-600 mb-1">Case Number (optional)</label>
              <input id="caseNumber" type="text" placeholder="e.g. T-777/25"
                class="w-full border border-gray-300 rounded-lg px-3 py-2 text-sm" />
            </div>
            <div>
              <label class="block text-xs font-medium text-gray-600 mb-1">Document Date (optional)</label>
              <input id="docDate" type="date"
                class="w-full border border-gray-300 rounded-lg px-3 py-2 text-sm" />
            </div>
          </div>
        </div>

        <button id="uploadBtn" disabled
          class="bg-blue-600 hover:bg-blue-700 disabled:bg-gray-300 disabled:cursor-not-allowed text-white font-medium px-6 py-2.5 rounded-lg text-sm transition">
          Upload &amp; Ingest
        </button>

        <!-- Progress list -->
        <div id="progressList" class="mt-6 space-y-2 hidden"></div>
      </div>

      <!-- ─── DOCUMENTS TAB ─── -->
      <div id="tab-documents" class="hidden">
        <div class="flex items-center justify-between mb-4">
          <h2 class="text-base font-semibold text-gray-800">Indexed Documents</h2>
          <button id="refreshDocsBtn" class="text-sm text-blue-600 hover:text-blue-800">↻ Refresh</button>
        </div>
        <div id="docsContent">
          <p class="text-gray-400 text-sm">Loading...</p>
        </div>
      </div>

      <!-- ─── SEARCH TAB ─── -->
      <div id="tab-search" class="hidden">
        <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6 mb-6">
          <div class="flex gap-3 mb-4">
            <input id="searchQuery" type="text" placeholder="Search your legal documents..."
              class="flex-1 border border-gray-300 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500" />
            <button id="searchBtn"
              class="bg-blue-600 hover:bg-blue-700 text-white font-medium px-5 py-2 rounded-lg text-sm transition shrink-0">
              Search
            </button>
          </div>

          <details class="text-sm">
            <summary class="text-gray-400 cursor-pointer select-none hover:text-gray-600">Filters</summary>
            <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 mt-3">
              <div>
                <label class="block text-xs font-medium text-gray-600 mb-1">Source Type</label>
                <select id="searchSourceType" class="w-full border border-gray-300 rounded-lg px-3 py-2 text-sm">
                  <option value="">All types</option>
                  <option value="olaf_complaint">OLAF Complaint</option>
                  <option value="general_court">General Court</option>
                  <option value="ias_audit">IAS Audit</option>
                  <option value="ep_discharge">EP Discharge</option>
                  <option value="correspondence">Correspondence</option>
                  <option value="staff_regulations">Staff Regulations</option>
                  <option value="decision">Decision</option>
                  <option value="other">Other</option>
                </select>
              </div>
              <div>
                <label class="block text-xs font-medium text-gray-600 mb-1">Case Number</label>
                <input id="searchCaseNumber" type="text" placeholder="e.g. T-777/25"
                  class="w-full border border-gray-300 rounded-lg px-3 py-2 text-sm" />
              </div>
              <div>
                <label class="block text-xs font-medium text-gray-600 mb-1">
                  Threshold: <span id="thresholdVal">0.60</span>
                </label>
                <input id="searchThreshold" type="range" min="0.50" max="1.00" step="0.05" value="0.60"
                  class="w-full accent-blue-600" />
              </div>
              <div>
                <label class="block text-xs font-medium text-gray-600 mb-1">Max Results</label>
                <input id="searchLimit" type="number" min="1" max="20" value="8"
                  class="w-full border border-gray-300 rounded-lg px-3 py-2 text-sm" />
              </div>
            </div>
          </details>
        </div>

        <div id="searchResults"></div>
      </div>

    </div>
  </main>
</div>

<script>
// ════════════════════════════════════════════════
//  Auth
// ════════════════════════════════════════════════
const getToken  = () => localStorage.getItem('rag_token');
const setToken  = t  => localStorage.setItem('rag_token', t);
const clearToken = () => localStorage.removeItem('rag_token');

async function apiFetch(path, opts = {}) {
  const res = await fetch(path, opts);
  if (res.status === 401) { clearToken(); showLogin(); return null; }
  return res;
}

function authHdr() {
  return { 'Authorization': `Bearer ${getToken()}` };
}

// ════════════════════════════════════════════════
//  Views
// ════════════════════════════════════════════════
function showLogin() {
  document.getElementById('loginView').classList.remove('hidden');
  document.getElementById('mainApp').classList.add('hidden');
}

function showApp() {
  document.getElementById('loginView').classList.add('hidden');
  document.getElementById('mainApp').classList.remove('hidden');
  switchTab('upload');
}

// ════════════════════════════════════════════════
//  Login
// ════════════════════════════════════════════════
document.getElementById('loginForm').addEventListener('submit', async e => {
  e.preventDefault();
  const password = document.getElementById('loginPassword').value;
  const errEl    = document.getElementById('loginError');
  errEl.classList.add('hidden');

  const res  = await fetch('/api/auth', {
    method:  'POST',
    headers: { 'Content-Type': 'application/json' },
    body:    JSON.stringify({ password }),
  });
  const data = await res.json();

  if (!res.ok) {
    errEl.textContent = data.error || 'Login failed';
    errEl.classList.remove('hidden');
    return;
  }
  setToken(data.token);
  showApp();
});

document.getElementById('logoutBtn').addEventListener('click', () => {
  clearToken();
  showLogin();
});

// ════════════════════════════════════════════════
//  Tabs
// ════════════════════════════════════════════════
function switchTab(name) {
  ['upload', 'documents', 'search'].forEach(t => {
    document.getElementById('tab-' + t).classList.toggle('hidden', t !== name);
  });
  document.querySelectorAll('.tab-btn').forEach(btn => {
    const active = btn.dataset.tab === name;
    btn.classList.toggle('border-b-2',        active);
    btn.classList.toggle('border-blue-600',   active);
    btn.classList.toggle('text-blue-600',     active);
    btn.classList.toggle('text-gray-500',    !active);
  });
  if (name === 'documents') loadDocuments();
}

document.querySelectorAll('.tab-btn').forEach(btn => {
  btn.addEventListener('click', () => switchTab(btn.dataset.tab));
});

// ════════════════════════════════════════════════
//  Upload tab
// ════════════════════════════════════════════════
const ALLOWED_EXTS = new Set(['.pdf', '.docx', '.txt', '.eml', '.msg']);
let selectedFiles  = [];

function extOf(name) { return name.slice(name.lastIndexOf('.')).toLowerCase(); }

function refreshFileUI() {
  const countEl    = document.getElementById('selectedCount');
  const uploadBtn  = document.getElementById('uploadBtn');
  const sourceType = document.getElementById('sourceType');

  uploadBtn.disabled = selectedFiles.length === 0;

  if (selectedFiles.length === 0) {
    countEl.classList.add('hidden');
    return;
  }
  countEl.classList.remove('hidden');
  countEl.textContent = `${selectedFiles.length} file(s) selected`;

  // Auto-select correspondence when ALL files are email types
  const allEmail = selectedFiles.every(f => ['.eml', '.msg'].includes(extOf(f.name)));
  if (allEmail) sourceType.value = 'correspondence';
}

function applyFiles(files) {
  selectedFiles = Array.from(files).filter(f => ALLOWED_EXTS.has(extOf(f.name)));
  refreshFileUI();
}

const fileInput   = document.getElementById('fileInput');
const folderInput = document.getElementById('folderInput');
const dropZone    = document.getElementById('dropZone');

document.getElementById('modeFiles').addEventListener('click', () => {
  selectedFiles = [];
  refreshFileUI();
  fileInput.value = '';
  fileInput.click();
});
document.getElementById('modeFolder').addEventListener('click', () => {
  selectedFiles = [];
  refreshFileUI();
  folderInput.value = '';
  folderInput.click();
});

fileInput.addEventListener('change',   () => applyFiles(fileInput.files));
folderInput.addEventListener('change', () => applyFiles(folderInput.files));

dropZone.addEventListener('click',    () => fileInput.click());
dropZone.addEventListener('dragover', e  => { e.preventDefault(); dropZone.classList.add('border-blue-400', 'bg-blue-50'); });
dropZone.addEventListener('dragleave',    () => dropZone.classList.remove('border-blue-400', 'bg-blue-50'));
dropZone.addEventListener('drop', e => {
  e.preventDefault();
  dropZone.classList.remove('border-blue-400', 'bg-blue-50');
  applyFiles(e.dataTransfer.files);
});

document.getElementById('uploadBtn').addEventListener('click', async () => {
  if (selectedFiles.length === 0) return;

  const sourceType = document.getElementById('sourceType').value;
  const caseNumber = document.getElementById('caseNumber').value;
  const docDate    = document.getElementById('docDate').value;
  const language   = document.getElementById('language').value;

  const progressList = document.getElementById('progressList');
  progressList.classList.remove('hidden');
  progressList.innerHTML = '';
  document.getElementById('uploadBtn').disabled = true;

  for (const file of selectedFiles) {
    const isEmail = ['.eml', '.msg'].includes(extOf(file.name));
    const row     = document.createElement('div');
    row.className = 'flex items-center gap-3 text-sm p-3 bg-gray-50 rounded-lg border border-gray-100';
    row.innerHTML = `
      <span class="flex-1 text-gray-700 truncate">${isEmail ? '📧' : '📄'} ${file.name}</span>
      <span class="status text-gray-400 shrink-0">Ingesting…</span>
    `;
    progressList.appendChild(row);
    const statusEl = row.querySelector('.status');

    try {
      const fd = new FormData();
      fd.append('file', file, file.name);
      fd.append('source_type', sourceType);
      if (caseNumber) fd.append('case_number', caseNumber);
      if (docDate)    fd.append('document_date', docDate);
      fd.append('language', language);

      const res  = await fetch('/api/ingest', {
        method:  'POST',
        headers: authHdr(),
        body:    fd,
      });
      const data = await res.json();

      if (!res.ok) {
        statusEl.textContent  = `✗ ${data.error}`;
        statusEl.className    = 'status text-red-500 shrink-0 text-xs';
      } else {
        statusEl.textContent  = `✓ ${data.chunksCreated} chunks`;
        statusEl.className    = 'status text-green-600 shrink-0 text-xs font-medium';
      }
    } catch {
      statusEl.textContent = '✗ Network error';
      statusEl.className   = 'status text-red-500 shrink-0 text-xs';
    }
  }

  selectedFiles     = [];
  fileInput.value   = '';
  folderInput.value = '';
  refreshFileUI();
});

// ════════════════════════════════════════════════
//  Documents tab
// ════════════════════════════════════════════════
async function loadDocuments() {
  const content = document.getElementById('docsContent');
  content.innerHTML = '<p class="text-gray-400 text-sm">Loading…</p>';

  const res = await apiFetch('/api/documents', { headers: authHdr() });
  if (!res) return;

  const docs = await res.json();

  if (docs.length === 0) {
    content.innerHTML = '<p class="text-gray-400 text-sm">No documents indexed yet. Upload some documents to get started.</p>';
    return;
  }

  content.innerHTML = `
    <div class="overflow-x-auto rounded-xl border border-gray-200">
      <table class="w-full text-sm">
        <thead class="bg-gray-50 text-gray-500 text-xs uppercase">
          <tr>
            <th class="px-4 py-3 text-left">ID</th>
            <th class="px-4 py-3 text-left">Filename</th>
            <th class="px-4 py-3 text-left">Type</th>
            <th class="px-4 py-3 text-left">Case</th>
            <th class="px-4 py-3 text-left">Date</th>
            <th class="px-4 py-3 text-left">Chunks</th>
            <th class="px-4 py-3 text-left"></th>
          </tr>
        </thead>
        <tbody id="docsTableBody" class="bg-white divide-y divide-gray-100"></tbody>
      </table>
    </div>
  `;

  const tbody = document.getElementById('docsTableBody');
  docs.forEach(doc => {
    const tr = document.createElement('tr');
    tr.className = 'hover:bg-gray-50';
    tr.innerHTML = `
      <td class="px-4 py-3 text-gray-400">${doc.id}</td>
      <td class="px-4 py-3 font-medium text-gray-800 max-w-xs">
        <span class="block truncate" title="${doc.filename}">${doc.filename}</span>
      </td>
      <td class="px-4 py-3 text-gray-500">${doc.source_type}</td>
      <td class="px-4 py-3 text-gray-500">${doc.case_number  || '—'}</td>
      <td class="px-4 py-3 text-gray-500">${doc.document_date || '—'}</td>
      <td class="px-4 py-3 text-gray-500">${doc.total_chunks}</td>
      <td class="px-4 py-3">
        <button class="delete-btn text-xs font-medium text-red-500 hover:text-red-700"
          data-id="${doc.id}" data-name="${doc.filename}">Delete</button>
      </td>
    `;
    tbody.appendChild(tr);
  });

  tbody.querySelectorAll('.delete-btn').forEach(btn => {
    btn.addEventListener('click', async () => {
      if (!confirm(`Delete "${btn.dataset.name}" and all its chunks? This cannot be undone.`)) return;
      const res = await apiFetch(`/api/documents/${btn.dataset.id}`, {
        method:  'DELETE',
        headers: authHdr(),
      });
      if (res) loadDocuments();
    });
  });
}

document.getElementById('refreshDocsBtn').addEventListener('click', loadDocuments);

// ════════════════════════════════════════════════
//  Search tab
// ════════════════════════════════════════════════
document.getElementById('searchThreshold').addEventListener('input', e => {
  document.getElementById('thresholdVal').textContent = parseFloat(e.target.value).toFixed(2);
});

async function doSearch() {
  const q = document.getElementById('searchQuery').value.trim();
  if (!q) return;

  const params = new URLSearchParams({ q });
  const st  = document.getElementById('searchSourceType').value;
  const cn  = document.getElementById('searchCaseNumber').value;
  const thr = document.getElementById('searchThreshold').value;
  const lim = document.getElementById('searchLimit').value;

  if (st)  params.set('source_type', st);
  if (cn)  params.set('case_number', cn);
  params.set('threshold', thr);
  params.set('limit', lim);

  const resultsEl = document.getElementById('searchResults');
  resultsEl.innerHTML = `
    <div class="flex items-center gap-2 text-gray-400 text-sm">
      <div class="w-4 h-4 border-2 border-blue-400 border-t-transparent rounded-full animate-spin"></div>
      Searching…
    </div>`;

  const res = await apiFetch(`/api/search?${params}`, { headers: authHdr() });
  if (!res) return;

  const results = await res.json();

  if (results.error) {
    resultsEl.innerHTML = `<p class="text-red-500 text-sm">${results.error}</p>`;
    return;
  }

  if (results.length === 0) {
    resultsEl.innerHTML = '<p class="text-gray-400 text-sm">No results found. Try lowering the threshold or broadening your query.</p>';
    return;
  }

  resultsEl.innerHTML = results.map(r => `
    <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-5 mb-4">
      <div class="flex items-start justify-between gap-4 mb-1">
        <span class="font-semibold text-gray-800 text-sm truncate">${r.filename}</span>
        <span class="shrink-0 text-xs font-bold text-blue-600 bg-blue-50 px-2 py-0.5 rounded-full">
          ${(r.similarity * 100).toFixed(1)}% match
        </span>
      </div>
      <p class="text-xs text-gray-400 mb-3">
        ${r.source_type}${r.case_number ? ' · ' + r.case_number : ''}${r.document_date ? ' · ' + r.document_date : ''}
      </p>
      <p class="text-sm text-gray-700 leading-relaxed whitespace-pre-wrap">${r.content.slice(0, 600)}${r.content.length > 600 ? '…' : ''}</p>
    </div>
  `).join('');
}

document.getElementById('searchBtn').addEventListener('click', doSearch);
document.getElementById('searchQuery').addEventListener('keydown', e => {
  if (e.key === 'Enter') doSearch();
});

// ════════════════════════════════════════════════
//  Init
// ════════════════════════════════════════════════
if (getToken()) {
  showApp();
} else {
  showLogin();
}
</script>
</body>
</html>
```

- [ ] **Step 3: Start the remote server locally and verify the UI loads**

In one terminal:
```bash
cd /Users/fred/Documents/VibeCoding/claudecode/mylegal-mcp && npm run start:remote 2>&1 &
```

In another (or after a 2-second pause):
```bash
curl -s http://localhost:3000/health
```

Expected: `{"status":"ok"}`

Then open `http://localhost:3000` in a browser. Expected: login page appears.

- [ ] **Step 4: Test login**

Enter your `ADMIN_PASSWORD` from `.env`. Expected: main app with Upload / Documents / Search tabs appears.

- [ ] **Step 5: Stop local server**

```bash
pkill -f "node.*remote" 2>/dev/null; pkill -f "tsx.*remote" 2>/dev/null; true
```

- [ ] **Step 6: Run tests one final time**

```bash
cd /Users/fred/Documents/VibeCoding/claudecode/mylegal-mcp && npm test 2>&1
```

Expected: 9/9 pass.

- [ ] **Step 7: Full TypeScript compile check**

```bash
cd /Users/fred/Documents/VibeCoding/claudecode/mylegal-mcp && npx tsc --noEmit 2>&1
```

Expected: no errors.

- [ ] **Step 8: Commit**

```bash
git add public/index.html
git commit -m "feat: add web management UI (upload, documents, search)"
```

---

## Self-Review

### Spec Coverage

| Spec Requirement | Task |
|---|---|
| Password auth — JWT, ADMIN_PASSWORD env var | Task 1 (env), Task 4 (api.ts) |
| POST /api/auth | Task 4 |
| GET /api/documents | Task 4 |
| DELETE /api/documents/:id | Task 4 |
| POST /api/ingest (multer, 20MB, all 5 types) | Task 4 |
| GET /api/search | Task 4 |
| parseEml — mailparser | Task 2 |
| parseMsg — @kenjiuno/msgreader | Task 2 |
| .eml/.msg branches in handleIngestFile | Task 3 |
| Mount /api router in remote.ts | Task 5 |
| Serve public/ as static | Task 5 |
| SPA fallback (excludes /api, /sse, /messages) | Task 5 |
| Login view | Task 6 |
| Upload tab — Files + Folder mode | Task 6 |
| Upload tab — sequential processing + progress list | Task 6 |
| Upload tab — auto-select correspondence for email files | Task 6 |
| Upload tab — drag-and-drop | Task 6 |
| Documents tab — table with delete + confirm | Task 6 |
| Search tab — query + filters + result cards | Task 6 |
| Tailwind CDN, vanilla JS, no build step | Task 6 |

All spec requirements covered. No gaps.
